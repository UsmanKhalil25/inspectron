{
  "name": "inspectron",
  "version": "1.0.0",
  "private": true,
  "description": "Inspectron monorepo",
  "workspaces": [
    "core",
    "agent-server",
     "ui"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  }
}

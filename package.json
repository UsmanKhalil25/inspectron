{
  "name": "inspectron",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ui",
    "core",
    "api"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "install:all": "npm install"
  }
}

# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  email: String!
  name: String!
  lastLoginAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type PublicUser {
  id: String!
  email: String!
  name: String!
  lastLoginAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

type LoginResponse {
  message: String!
  user: PublicUser!
}

type RegisterResponse {
  message: String!
  data: PublicUser!
}

type PaginationInfo {
  page: Int!
  limit: Int!
  total: Int!
  totalPages: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type Project {
  id: ID!
  name: String!
  url: String!
  description: String
  user: PublicUser!
  scanCount: Int!
  lastScanStatus: String
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ActionDetail {
  name: String!
  params: String!
  display: String!
}

type ActionContext {
  url: String!
  title: String!
}

type ScanAction {
  step: Int!
  timestamp: String!
  thinking: String
  action: ActionDetail!
  context: ActionContext!
}

type Vulnerability {
  id: ID!
  findingId: String!
  title: String!
  severity: VulnerabilitySeverity!
  category: VulnerabilityCategory!
  url: String!
  description: String!
  evidence: String!
  remediation: String!
  scanId: String!
}

enum VulnerabilitySeverity {
  CRITICAL
  HIGH
  MEDIUM
  LOW
  INFO
}

enum VulnerabilityCategory {
  SECURITY_HEADERS
  SENSITIVE_FILES
  COOKIES
  CSRF
  INFORMATION_DISCLOSURE
  XSS
  SQL_INJECTION
}

type Scan {
  id: ID!
  url: String!
  status: ScanStatus!
  scanType: ScanType!
  runId: String
  result: String
  actions: [ScanAction!]
  vulnerabilities: [Vulnerability!]
  user: PublicUser!
  project: Project!
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum ScanStatus {
  DRAFT
  QUEUED
  ACTIVE
  COMPLETED
  FAILED
}

enum ScanType {
  STATIC
  DYNAMIC
}

type ScansResponse {
  scans: [Scan!]!
  pagination: PaginationInfo!
}

type ScanStatusStats {
  draft: Int!
  queued: Int!
  active: Int!
  completed: Int!
  failed: Int!
}

type ScanStats {
  totalScans: Int!
  scansByStatus: ScanStatusStats!
}

type VulnerabilitySeverityStats {
  severity: VulnerabilitySeverity!
  count: Int!
}

type VulnerabilityCategoryStats {
  category: VulnerabilityCategory!
  count: Int!
}

type VulnerabilityStats {
  total: Int!
  bySeverity: [VulnerabilitySeverityStats!]!
  byCategory: [VulnerabilityCategoryStats!]!
}

type ScanEvent {
  scanId: ID!
  type: String!
  data: ScanAction
  result: String
  message: String
  timestamp: String
}

type BrowserPreviewFrame {
  runId: ID!
  frame: String!
  timestamp: Float!
  frameNumber: Int!
  latencyMs: Int!
  url: String
}

type ProjectsResponse {
  projects: [Project!]!
  pagination: PaginationInfo!
}

type ProjectVulnerabilityStats {
  projectId: ID!
  projectName: String!
  critical: Int!
  high: Int!
  medium: Int!
  low: Int!
  info: Int!
  total: Int!
}

type Query {
  project(id: String!): Project!
  projects(page: Int = 1, limit: Int = 20, filters: ProjectFiltersInput): ProjectsResponse!
  projectVulnerabilityStats: [ProjectVulnerabilityStats!]!
  scan(id: String!): Scan!
  scans(page: Int = 1, limit: Int = 20, filters: ScanFiltersInput): ScansResponse!
  scanStats: ScanStats!
  vulnerabilityStats: VulnerabilityStats!
  scanScreenshot(runId: String!): String
  currentUser: User
}

input ProjectFiltersInput {
  search: String
  sortBy: ProjectSortBy = CREATED_AT
  sortOrder: SortOrder = DESC
}

enum ProjectSortBy {
  CREATED_AT
  UPDATED_AT
  NAME
}

enum SortOrder {
  ASC
  DESC
}

input ScanFiltersInput {
  search: String
  status: ScanStatus
  projectId: String
  createdAfter: String
  createdBefore: String
  sortBy: ScanSortBy = CREATED_AT
  sortOrder: SortOrder = DESC
}

enum ScanSortBy {
  CREATED_AT
  UPDATED_AT
  URL
}

type Mutation {
  createProject(input: CreateProjectInput!): Project!
  updateProject(id: String!, input: UpdateProjectInput!): Project!
  deleteProject(id: String!): Boolean!
  createScan(input: CreateScanInput!): Scan!
  startScan(id: String!): Scan!
  sendAgentMessage(input: SendMessageInput!): Boolean!
  login(input: LoginUserInput!): LoginResponse!
  register(input: RegisterUserInput!): RegisterResponse!
}

input CreateProjectInput {
  name: String!
  url: String!
  description: String
}

input UpdateProjectInput {
  name: String
  url: String
  description: String
}

input CreateScanInput {
  projectId: String!
  url: String
  status: ScanStatus
  scanType: ScanType
}

input SendMessageInput {
  scanId: String!
  content: String!
}

input LoginUserInput {
  email: String!
  password: String!
}

input RegisterUserInput {
  email: String!
  name: String!
  password: String!
  confirmPassword: String!
}

type Subscription {
  scanStatusChanged(scanId: String!): Scan!
  scanEvents(scanId: String!): ScanEvent!
  browserPreviewStream(runId: String!): BrowserPreviewFrame!
}